$('.dialog').html('<%= j(render :partial => "lists/new")%>')
            .dialog({
              maxHeight: 550,
              modal: true,
              resizable: false,
              title: "New List",
              width: 600
            });