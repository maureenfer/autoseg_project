$('.dialog').html('<%= j(render :partial => "tasks/new")%>')
            .dialog({
              maxHeight: 550,
              modal: true,
              resizable: false,
              title: "New Task",
              width: 600
            });