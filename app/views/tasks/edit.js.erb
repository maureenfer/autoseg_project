$('.dialog').html('<%= j(render :partial => "tasks/edit")%>')
            .dialog({
              maxHeight: 550,
              modal: true,
              resizable: false,
              title: "Edit Task",
              width: 600
            });